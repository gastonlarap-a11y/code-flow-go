## NIVEL DE REVISIÓN ACTIVO: completo
The working default: the first five lenses, and a bar that lets a solid edge case through. Report findings with confidence ≥ 60 (`Blocker` with ≥ 50). Include `Blocker`/`Crítico`/`Mayor`/`Menor`; leave `Info` out unless it is clearly useful. Do not invent trivial findings when the code is sound.